
#to run mongo
sudo docker run -p 27017:27017 --name mongo -d mongo

#move to server code
docker build -t shubhag/node-mongo-server .

#push if wanted
docker login
docker push shubhag/...

#link and run server code
docker run -p 3001:80 --name web1 --link mongo:mongo shubhag/node-mongo-server
docker run -p 3002:80 --name web2 --link mongo:mongo shubhag/node-mongo-server
docker run -p 3003:80 --name web3 --link mongo:mongo shubhag/node-mongo-server

#now run haproxy
docker run -d -p 80:80 --link web1 --link web2 --link web3 tutum/haproxy






#to remove docker image
docker rm -f web1

#to get last created container
docker ps -l